{
  "_recorded": {
    "at": "hand-written",
    "endpoint": "api/heb/session/status",
    "sanitised": true,
    "note": "mock-backend keys BOTH clip routes off the same clipState, so every health.<state>.json needs a session-status sibling or api/heb/session/status 404s as unmocked. An expiring login still works, so this stays the healthy shape."
  },
  "active": false,
  "loginSessionValid": true,
  "idleSeconds": 0
}
