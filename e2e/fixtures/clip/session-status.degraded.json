{
  "_recorded": {
    "at": "hand-written",
    "endpoint": "api/heb/session/status",
    "sanitised": true,
    "note": "mock-backend keys BOTH clip routes off the same clipState, so every health.<state>.json needs a session-status sibling or api/heb/session/status 404s as unmocked. A database outage says nothing about the browser session, so this stays the healthy shape."
  },
  "active": false,
  "loginSessionValid": true,
  "idleSeconds": 0
}
